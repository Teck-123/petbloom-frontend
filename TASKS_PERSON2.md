# Tasks for Person 2 - Easy Work

## Your Job: Add comments to pages

### Step 1: Setup (Copy-paste these commands)
```bash
git checkout dev
git pull origin dev
git checkout -b feature/person2-page-docs
```

### Step 2: Do These Simple Tasks (Each is 1 commit)

#### Task A: Add comment to Home.jsx
Open `src/pages/Home.jsx`
Add this comment at the top (line 1):
```javascript
// Home page - Landing page with featured pets and products
```
Then run:
```bash
git add src/pages/Home.jsx
git commit -m "Add documentation comment to Home page"
```

#### Task B: Add comment to Login.jsx
Open `src/pages/Login.jsx`
Add this comment at the top (line 1):
```javascript
// Login page - User authentication with email/password and Google
```
Then run:
```bash
git add src/pages/Login.jsx
git commit -m "Add documentation comment to Login page"
```

#### Task C: Add comment to Pets.jsx
Open `src/pages/Pets.jsx`
Add this comment at the top (line 1):
```javascript
// Pets page - Browse and search available pets
```
Then run:
```bash
git add src/pages/Pets.jsx
git commit -m "Add documentation comment to Pets page"
```

#### Task D: Add comment to Cart.jsx
Open `src/pages/Cart.jsx`
Add this comment at the top (line 1):
```javascript
// Cart page - Shopping cart with checkout functionality
```
Then run:
```bash
git add src/pages/Cart.jsx
git commit -m "Add documentation comment to Cart page"
```

### Step 3: Push to GitHub
```bash
git push origin feature/person2-page-docs
```

### Step 4: Create Pull Request
1. Go to GitHub repository
2. Click "Compare & pull request"
3. Make sure base is `dev`
4. Click "Create Pull Request"

✅ DONE! You now have 4 commits showing your work!
